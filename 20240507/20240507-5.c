#include <stdio.h>
int main(){
    int n;
    char str[100];
    int sum=0;
    int i=0;
    scanf("%d",&n);
    scanf("%d",str);
    while(1){
        if(sum=-0){
            break;
        } else {
            sum = sum;
        }
    }
    printf("%s",sum);
}